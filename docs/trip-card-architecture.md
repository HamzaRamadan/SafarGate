# وثيقة الهندسة الفنية: شجرة بطاقة الرحلة الموحدة (ARTA-008)

هذا المستند هو المرجع السيادي الرسمي الذي يوثق ويحلل البنية الفنية الكاملة لـ "بطاقة الرحلة المجدولة" وكيفية ظهورها وتصرفها بشكل مختلف تماماً لكل من "الناقل" و "المسافر".

---

### 1. شجرة الرسم التقني: تشريح البطاقة المزدوج

هذا الرسم يوضح كيف أن "وثيقة الرحلة" الواحدة في قاعدة البيانات يتم عرضها بطريقتين: كـ "وحدة قيادة" للناقل، و كـ "بطاقة دعائية" للمسافر.

```
/ (نظام سفريات)
│
├── 👨‍✈️ **الطرف الأول: الناقل (وحدة القيادة والتحكم)**
│   │
│   ├── 1. **التأسيس:** `src/app/carrier/add-trip-dialog.tsx`
│   │   └── ➡️ إنشاء وثيقة `Trip` كاملة بالبيانات.
│   │
│   └── 2. **الإدارة (The Command Unit):** `src/components/carrier/my-trips-list.tsx`
│       ├── 📥 **المدخلات:** `trip` (كائن الرحلة).
│       ├── ⚙️ **درع المقاعد:** أزرار (+/-) لتعديل `availableSeats` بشكل آمن.
│       ├── ⚖️ **مشرّع القانون (SC-040):**
│       │   ├── `hasPassengers`? -> زر `ArrowRightLeft` (فتح `TransferRequestDialog`).
│       │   └── `else` -> زر `Trash2` (فتح `AlertDialog` للإلغاء).
│       │
│       └── 📖 **كشف الركاب (Accordion - Protocol 88):**
│           └── **[مدمج]** <BookingActionCard />
│               └── 💬 **قناة الاتصال الموحدة (SC-038):**
│                   └── زر "الدردشة" يفتح `ChatDialog` الداخلي.
│
└── 🧳 **الطرف الثاني: المسافر (واجهة الحجز)**
    │
    ├── 🌐 **المحرك:** `src/app/dashboard/page.tsx`
    │   └── يجلب الرحلات ويعرضها باستخدام "بطاقة الحجز".
    │
    └── 💳 **واجهة الاشتباك:** `src/components/scheduled-trip-card.tsx`
        ├── 📥 **المدخلات:** `trip` (كائن الرحلة).
        ├── 🛡️ **عامل الثقة:** عرض بيانات الناقل وسمعته (`CarrierInfo`).
        └── 🚀 **المشغّل:** زر "حجز مقعد" يطلق `onBookNow()`.

```

---

### 2. التحليل الشمولي (Holistic Analysis)

#### 2.1. بطاقة الناقل: "غرفة عمليات" مصغرة

بفضل الأوامر من **SC-033 إلى SC-040**، لم تعد بطاقة الناقل مجرد واجهة عرض، بل تحولت إلى "وحدة قيادة وتحكم" متكاملة.

*   **الوظيفة:** ليست للعرض، بل **للإدارة**. تمكن الناقل من السيطرة الكاملة على رحلته.
*   **القدرات المكتسبة (نتيجة الأوامر):**
    *   **التحكم التشغيلي:** إدارة المقاعد المتاحة بشكل فوري.
    *   **فرض القانون (SC-040):** استبدال زر الإلغاء العشوائي بمنطق شرطي صارم يفرض "نقل الركاب" كحل وحيد عند وجود حجوزات، مما يحمي حقوق المسافرين وموثوقية المنصة.
    *   **الاستخبارات عند الطلب (SC-033 & Protocol 88):** "كشف الركاب والمالية" (Accordion) لا يقوم بجلب أي بيانات إضافية إلا عند ضغط الناقل عليه، مما يوفر آلاف القراءات غير الضرورية من قاعدة البيانات ويحمي الموارد.
    *   **الاتصال الموحد (SC-038):** تم استئصال رابط WhatsApp الخارجي وزرع نظام الدردشة الداخلي، مما يجعل كل الاتصالات موثقة ومركزية داخل النظام.

#### 2.2. بطاقة المسافر: "أداة قرار" سريعة

على الجانب الآخر، بقيت بطاقة المسافر بسيطة ومركزة على هدف واحد.

*   **الوظيفة:** **الإقناع والحجز**. تزويد المسافر بالمعلومات الكافية لاتخاذ قرار سريع.
*   **العوامل الحاسمة:**
    *   **الثقة:** إبراز تقييم الناقل وصورته (`CarrierInfo`).
    *   **الوضوح:** عرض السعر والمقاعد المتاحة وشروط الرحلة بوضوح.
    *   **سهولة الاستخدام:** زر "حجز مقعد" كبير وواضح كإجراء وحيد.

---

### 3. خلاصة معمارية

النظام يفصل بشكل صارم بين "واجهة الإدارة" للناقل و "واجهة الحجز" للمسافر. هذا الفصل السيادي يضمن:
1.  **عدم التضارب:** لا يمكن للناقل رؤية واجهة المسافر البسيطة، ولا يمكن للمسافر الوصول إلى أدوات التحكم الخاصة بالناقل.
2.  **الكفاءة:** كل واجهة مصممة بدقة لتخدم غرضها دون أي تكديس أو ميزات غير ضرورية.
3.  **الأمان:** عزل منطق الإدارة الحساس في جانب الناقل فقط يحمي النظام من أي تلاعب محتمل.

هذه الوثيقة تؤرشف التحول الاستراتيجي الذي تم، وتحصّن المكتسبات التقنية التي حققناها.