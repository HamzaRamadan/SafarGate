# وثيقة الهندسة الفنية: شجرة لوحة تحكم المسافر (ARTA-006)

هذا المستند هو المرجع السيادي الرسمي للبنية التقنية والهيكلية للوحة تحكم المسافر، وهي نقطة الانطلاق والتحكم الرئيسية لجميع عمليات المسافرين في نظام "سفريات".

---

### 1. شجرة الرسم التقني (Technical Architecture Tree)

هذا الرسم يوضح البنية الهيكلية والمترابطة لشاشة المسافر، مع الإشارة إلى الملفات المسؤولة عن كل جزء وظيفي.

```
/dashboard (نقطة الدخول الرئيسية للمسافر) -> [src/app/dashboard/page.tsx]
│
├── 🧠 **العقل المدبر (Main Component Logic):**
│   ├── Hooks: `useUser`, `useFirestore`, `useRouter`, `useToast` -> (إدارة الجلسة، الاتصال، التوجيه، الإشعارات)
│   ├── State: `useState`, `useEffect`, `useMemo` -> (إدارة حالة الواجهة والبيانات المحلية)
│   └── Data Queries: `useCollection` -> (جلب بيانات الرحلات والناقلين بشكل حي)
│
├── 🔬 **كبسولة البحث الذكي (Smart Search Capsule):** -> [داخل `page.tsx`]
│   ├── <Accordion>
│   │   ├── 🚗 **فلتر نوع المركبة:** [Button Group] -> (تحديث حالة `searchVehicleType`)
│   │   ├── 🚚 **فلتر الناقل:** [Select Menu] -> (تحديث حالة `selectedCarrier` و `searchMode`)
│   │   └── 🌍 **فلاتر الوجهة والتاريخ (ذكية):**
│   │       ├── 1. اختيار دولة الانطلاق.
│   │       ├── 2. [خوارزمية availableDestinations]: تقوم بمسح الرحلات وعرض دول الوجهة المتاحة فقط.
│   │       └── 3. اختيار باقي الفلاتر (مدينة، تاريخ، مقاعد).
│   └── ➡️ **الأثر:** كل تغيير هنا يُفعّل `useMemo` لإعادة حساب `tripDisplayResult` وعرض النتائج المفلترة فوراً.
│
├── 🌐 **شبكة النتائج (Results Grid):** -> [داخل `page.tsx`]
│   ├── 🔄 **حالة التحميل:** [Skeleton Components]
│   ├── 🚫 **حالة "لا توجد نتائج":** [ShipWheel Icon & Message]
│   └── ✅ **حالة وجود نتائج (مباشرة أو بديلة):**
│       └── <ScheduledTripCard /> -> [src/components/scheduled-trip-card.tsx]
│           └── ➡️ **الإجراء:** `onBookNow()` -> تفعيل `BookingDialog`
│
└── 💬 **النوافذ الحوارية التفاعلية (Interactive Dialogs):**
    ├── <BookingDialog /> -> [src/components/booking-dialog.tsx] -> (جمع بيانات الركاب)
    │   └── **[ORD-019] ➡️** `handleConfirmBooking` -> `router.push('/history')` (إغلاق الحلقة)
    │
    ├── <RequestDialog /> -> [src/components/requests/request-dialog.tsx] -> (إرسال طلب رحلة خاص)
    └── <AuthRedirectDialog /> -> [src/components/auth-redirect-dialog.tsx] -> (توجيه المستخدم غير المسجل للدخول)

```

---

### 2. التحليل الشمولي (Holistic Analysis)

#### 2.1. مواطن القوة (Strengths)

*   **بنية مرنة وقابلة للتطوير (Flexible & Scalable):**
    *   فصل منطق عرض الرحلة (`ScheduledTripCard`) عن منطق التحكم والفلترة (`page.tsx`) يسمح بتطوير كل جزء بشكل مستقل دون التأثير على الآخر.
    *   الاعتماد على `useMemo` للفلترة يضمن أداءً عالياً حتى مع زيادة عدد الرحلات، حيث لا يتم إعادة حساب النتائج إلا عند الضرورة.

*   **تجربة مستخدم ذكية (Intelligent UX):**
    *   **الفلترة الذكية للمسارات:** الواجهة لم تعد تعرض وجهات فارغة. خوارزمية `availableDestinations` تضمن أن المستخدم يرى فقط المسارات التي توجد رحلات فعلية لها، مما يقلل من الإحباط ويسرّع عملية اتخاذ القرار.
    *   **عرض النتائج البديلة:** عند عدم وجود تطابق تام، يعرض النظام رحلات قريبة في التوقيت أو المسار، وهو تطبيق ذكي يقلل من فرص مغادرة المستخدم للتطبيق.

*   **تسلسل وظيفي مغلق (Closed Functional Loop):**
    *   **إصلاح [ORD-019]:** تم سد الثغرة في تجربة المستخدم. بعد أن يؤكد المسافر الحجز، يتم الآن إظهار رسالة نجاح واضحة ونقله تلقائياً إلى صفحة متابعة الحجوزات (`/history`). هذا يغلق الحلقة بشكل كامل ويزيل أي شعور بعدم اليقين.

#### 2.2. مواطن الضعف والتحديات المستقبلية (Weaknesses & Future Challenges)

*   **لا توجد نقاط ضعف حرجة حالياً.** البنية الحالية قوية ومستقرة.
*   **تحدي مستقبلي:** مع زيادة عدد الرحلات والناقلين بشكل كبير، قد تحتاج عملية الفلترة (`tripDisplayResult`) إلى مزيد من التحسين، ربما عن طريق نقل جزء من منطق الفلترة إلى الخادم (Server-side) لتقليل العبء على جهاز العميل، ولكن هذا يعتبر تطويراً مستقبلياً وليس ضعفاً حالياً.

---

هذا المستند يمثل الحالة النهائية والمثالية للوحة تحكم المسافر.
